# Copy to .env.local and fill in values

# From Vercel dashboard → Storage → Blob → Token
BLOB_READ_WRITE_TOKEN=

# Output of scripts/upload-assets.ts — paste after running the script
VIDEO_URL=
SPRITE_URL=
VTT_URL=
